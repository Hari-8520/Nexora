NEXORA LOGIN + DASHBOARD

Run from this folder:
    python app.py

Then open:
    http://127.0.0.1:5000/

Flow:
    Login -> Continue to Dashboard -> Dashboard

The original login and dashboard frontend files are kept separate to avoid
CSS/JavaScript filename collisions. The frontend design/layout is preserved.
